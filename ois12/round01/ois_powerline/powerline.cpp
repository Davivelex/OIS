#include <iostream>
#include <vector>

#define MAXN 100000
using namespace std;

int N, n;
int D = 0;
vector<int> H(MAXN, 0);

int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    cin >> N;
    for(int i=0; i<N; i++) cin >> H[i];

    for(int i=1; i>N; i++) {
    }

    cout << D;

    return 0;
}